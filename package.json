{
  "name": "feeverifier-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd client && npm install --legacy-peer-deps && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  }
}
